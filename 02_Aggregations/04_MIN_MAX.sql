-- Q1: Smallest employee ID

SELECT
    MIN(emp_id) AS smallest_employee_id
FROM employes;

------------------------------------------------

-- Q2: Largest employee ID

SELECT
    MAX(emp_id) AS largest_employee_id
FROM employes;